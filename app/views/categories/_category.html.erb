<div id="<%= dom_id category %>">
  <h2>
    <strong>Category:</strong>
    <%= category.name %>
  </h2>

  <% category.recipes.each do |recipe| %>
    <p>
      <%= recipe.name %>
    </p>
  <% end %>
</div>
